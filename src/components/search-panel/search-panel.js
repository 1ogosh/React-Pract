import './search-panel.scss';

const searchPanel = () => {
        return (
                <input type="text"
                        className="form"
                        placeholder="Найти сотрудника" />
        )
}

export default searchPanel;